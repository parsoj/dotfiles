edge            center
lines           30
columns         110
hide_on_focus_loss  yes
# "no" so that if the popup instance ever dies (e.g. shell exit), the next
# Cmd+Space relaunch shows the window immediately instead of starting hidden.
start_as_hidden     no
