function pull() {

         currPath=$(pwd)
         pathArray=(${currPath//// })

         #rsync over standard port of 873
         rsync -e "ssh -p 4000" -r --compress --progress jeff.parsons@localhost:$currPath $currPath


}
