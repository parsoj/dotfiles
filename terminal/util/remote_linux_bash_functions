function pull() {

         currPath=$(pwd)
         pathArray=(${currPath//// })

         #rsync over 4000
         rsync -avzh --compress-level=9 -e "ssh -p 4000" -r --progress jeff.parsons@127.0.0.1:$currPath $currPath


}
