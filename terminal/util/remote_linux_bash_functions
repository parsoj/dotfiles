function pull() {

         currPath=$(pwd)
         pathArray=(${currPath//// })

         #rsync over standard port of 873
         rsync -avzh -e "ssh -p 4000" -r --progress jeff.parsons@localhost:$currPath $currPath


}
