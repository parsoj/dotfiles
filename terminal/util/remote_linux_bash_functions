function pull() {

         currPath=$(pwd)
         pathArray=(${currPath//// })

         #rsync over standard port of 873
         rsync -avzhe ssh --progress jeff.parsons@localhost$currPath $currPath


}
